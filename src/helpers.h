#ifndef HELPERS_H
#define HELPERS_H

#include <vector>
#include <algorithm>
#include <string>

class Helpers
{
public:
  static bool isInVector(std::vector<std::string> vector, std::string string);
};

#endif
